namespace FileToImage.Threading
{
    public class ReadWorker
    {
        // Worker class for reading.
        /*
         * Will get a section of the rgb arrays from the image.
         * Then it will combine those arrays into a single long array that is then passed back to the ImageReader.
         */
    }
}